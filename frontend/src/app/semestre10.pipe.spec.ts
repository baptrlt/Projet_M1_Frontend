import { Semestre10Pipe } from './semestre10.pipe';

describe('Semestre10Pipe', () => {
  it('create an instance', () => {
    const pipe = new Semestre10Pipe();
    expect(pipe).toBeTruthy();
  });
});
